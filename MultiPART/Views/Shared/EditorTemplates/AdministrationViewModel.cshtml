@model MultiPART.Models.ViewModel.AdministrationViewModel
<div class="row col-xs-12 form-group">

@*    @using (Html.BeginForm("Update", "Administration", new { projectid = Model.ProjectID, procedureid = Model.ProcedureID, administrationid = Model.AdministrationID, starttime = Model.StartTime, endtime = Model.EndTime }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
     *@
        @Html.HiddenFor(m => m.ProjectID)
        @Html.HiddenFor(m => m.ProcedureID)
        @Html.HiddenFor(m => m.AdministrationID)
        
       <div class="col-sm-3 col-xs-4 col-sm-offset-4 col-md-offset-3">
            @Html.TextBoxFor(model => model.StartTime, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.StartTime)
        </div>

        <div class="col-sm-3 col-xs-4 col-md-offset-1">
            @Html.TextBoxFor(model => model.EndTime, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.EndTime)
        </div>
       @* <div class="col-sm-1 col-xs-2">
            <button type="submit" class="btn btn-info "><i class="fa fa-refresh"></i></button>
        </div>*@
    @*}*@

    <div class="col-sm-1 col-xs-2">

@*        @using (Html.BeginForm("Delete", "Administration", new { administrationid = Model.AdministrationID, procedureid = Model.ProcedureID, projectid = Model.ProjectID }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)

            <button type="submit" class="btn btn-danger "><i class="fa fa-trash"></i></button>
        }*@

    </div>
</div>
